# Numerals control render
Five constructions of the number ten. `result.png` is the engine's output.
`١٠` plain → `٠١`. With `direction:ltr` → still `٠١`. Pre-reversed → `١٠` (fragile). Western `10` → `10`. `١٢٠` → `٠٢١`.
→ Use Western digits. See `references/arabic-captions.md` §2.
